after post_type register see if need https://codex.wordpress.org/Function_Reference/register_post_type#Example

we need quary to show post in front end

.....................


query_posts( array(
    'category_name'  => 'my-category-slug',
    'posts_per_page' => -1
) );

then

 if(have_posts()): while ( have_posts() ) : the_post();
    echo '
<li>';
    the_title();
  the_content();
    echo '</li>
 
';
// see
endwhile;
